services:
  - type: web
    name: stock-analyzer
    runtime: python

    buildCommand: pip install -r requirements.txt

    startCommand: gunicorn src.stock_analyser_pro.main:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
